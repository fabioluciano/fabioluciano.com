---
// =============================================================================
// Root Index - Redirects to default locale
// =============================================================================

// In SSG mode, we redirect to the default locale
// The actual home page content is in /[lang]/index.astro
return Astro.redirect('/pt');
---
